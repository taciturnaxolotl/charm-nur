# Scripts

Right now we just have one but if we get more in the future they can be documented here

## Crush module options update

This fetches the json schema for crush and then coverts it to nix module options. Run as follows:

```bash
./scripts/generate-options.sh > modules/crush/options.nix
```
